import 'package:flutter/material.dart';

class RepublicCard extends StatelessWidget {
  const RepublicCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
